cubes:
  - name: rpt_malaria_age_groups
    sql_table: "\"REPORTING\".\"RPT_MALARIA_AGE_GROUPS\""
    data_source: default

    joins: []

    dimensions:
      - name: age_group_label
        sql: "{CUBE}.\"AGE_GROUP_LABEL\""
        type: string

      - name: age_group_raw
        sql: "{CUBE}.\"AGE_GROUP_RAW\""
        type: string

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

