cubes:
  - name: rpt_malaria_care_cascade
    sql_table: "\"REPORTING\".\"RPT_MALARIA_CARE_CASCADE\""
    data_source: default

    joins: []

    dimensions:
      - name: branch
        sql: "{CUBE}.\"BRANCH\""
        type: string

      - name: branch_label
        sql: "{CUBE}.\"BRANCH_LABEL\""
        type: string

      - name: confidence_level
        sql: "{CUBE}.\"CONFIDENCE_LEVEL\""
        type: string

      - name: source_schema
        sql: "{CUBE}.\"SOURCE_SCHEMA\""
        type: string

      - name: step_name
        sql: "{CUBE}.\"STEP_NAME\""
        type: string

      - name: zone_label
        sql: "{CUBE}.\"ZONE_LABEL\""
        type: string

    measures:
      - name: count
        type: count

      - name: dropout_count
        sql: "{CUBE}.\"DROPOUT_COUNT\""
        type: sum

      - name: prior_step_count
        sql: "{CUBE}.\"PRIOR_STEP_COUNT\""
        type: sum

      - name: step_count
        sql: "{CUBE}.\"STEP_COUNT\""
        type: sum

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

